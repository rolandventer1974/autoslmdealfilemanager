kind = "web"
previewPath = "/"
title = "AutoSLM Deal File Manager"
version = "1.0.0"
id = "artifacts/deal-file-manager"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19357

[services.development]
run = "pnpm --filter @workspace/deal-file-manager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/deal-file-manager", "run", "build" ]
publicDir = "artifacts/deal-file-manager/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19357"
BASE_PATH = "/"
